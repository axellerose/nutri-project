{{#if errorMessage}}
  <div>
      <p>{{errorMessage}}</p>
  </div>   
{{/if}}

<form id="create-recipe-form" class="create-form" action="/recipes/create" method="POST">
  <h2>Create a recipe</h2>
  <div class="input-container">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" placeholder="Ex: Pasta Bolognese">
  </div>

  <div class="input-container">
    <label for="img">Image:</label>
    <input type="text" id="img" name="image" placeholder="Image URL">
  </div>
  
  <div id="time-container">
    <label for="time">Cooking time</label>
    <input type="number" id="time" name="time" placeholder="min">
  </div>

  <div class="list-container">
    <label style="padding-right:20px;">Ingredients</label><span>Quantity</span>
    <ul id="ingredients-list">
    </ul>
  </div>

  <div class="input-container">
    <select id="products" name="products">
      {{#each products}}
        <option value="{{_id}}">{{this.name}}</option>
      {{/each}}
    </select>
  </div>

  <div class="input-container">
    <label for="quantity" style="padding-right:20px;">Quantity</label><br>
    <input type="number" id="quantity" name="quantity" placeholder="in grams" min=0>
    <button class="button-in-form" id="new-ingredient">Add</button>
  </div>
  
  <label for="steps">Steps:</label>
  <textarea id="steps" name="steps" rows="10" cols="30">
    Write here the different steps for this recipe
  </textarea>

  <div id="list-to-send">
    {{!-- Will be filled dynamically with JS --}}
  </div>

  <button class="button-in-form create" type="submit" id="create-recipe-button" disabled>Create recipe</button><br>
</form>